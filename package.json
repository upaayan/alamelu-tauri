{
  "name": "alamelu-pi-gui",
  "version": "0.1.0",
  "private": true,
  "description": "Alamelu Pi desktop shell over the installed pi command.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "build": "pnpm --filter @pi-gui/desktop run build",
    "typecheck": "pnpm --filter @pi-gui/desktop run typecheck",
    "package:alpi:dir": "pnpm --filter @pi-gui/desktop run package:alpi:dir",
    "verify:external-runtime": "pnpm --filter @pi-gui/desktop run verify:external-runtime",
    "verify:packaged-runtime-deps": "pnpm --filter @pi-gui/desktop run verify:packaged-runtime-deps"
  }
}
